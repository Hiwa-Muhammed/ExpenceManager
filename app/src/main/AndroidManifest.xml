<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expencemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenceManager">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home"
            android:label="Loginned As"
            android:theme="@style/Theme.Design.NoActionBar"
            > </activity>
        <activity android:name=".AddExpences"
            android:label="Loginned As"
            android:theme="@style/Theme.Design.NoActionBar"
            > </activity>
        <activity android:name=".adminHome"
            android:theme="@style/Theme.Design.NoActionBar"
            > </activity>
        <activity android:name=".IncomesManager"
            android:theme="@style/Theme.Design.NoActionBar"
            > </activity>
        <activity android:name=".EditIncome"
            android:theme="@style/Theme.Design.NoActionBar"
            > </activity>
        <activity android:name=".TransactionDetailsEmployee"
            android:theme="@style/Theme.Design.NoActionBar"
            > </activity>
        <activity android:name=".ExpenseDetails"
            android:theme="@style/Theme.Design.NoActionBar"
            > </activity>
        <activity android:name=".TransactionDetailsAdmin"
            android:theme="@style/Theme.Design.NoActionBar"
            > </activity>
    </application>

</manifest>